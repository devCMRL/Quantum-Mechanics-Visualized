# Density functional theory (DFT) track

This package holds code and documentation for a **separate** DFT curriculum. It is **not** imported by the current Streamlit entry point.

Integration when ready: add chapter scripts under `pages/`, extend `requirements.txt` (or add a split requirements file) for DFT-related dependencies, and implement reusable routines in `dft_course/`.
